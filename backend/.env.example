# LLM Provider Configuration
# Options: 'openai' | 'azure-openai-key' | 'azure-openai-identity'
LLM_PROVIDER=openai

# Model name (for OpenAI) or deployment name fallback
LLM_MODEL=gpt-4o

# ========================================
# OpenAI Configuration (LLM_PROVIDER=openai)
# ========================================
OPENAI_API_KEY=your-openai-api-key

# ========================================
# Azure OpenAI Configuration
# Required for both 'azure-openai-key' and 'azure-openai-identity'
# ========================================
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=your-deployment-name
AZURE_OPENAI_API_VERSION=2024-08-01-preview

# Azure OpenAI API Key (only for LLM_PROVIDER=azure-openai-key)
AZURE_OPENAI_API_KEY=your-azure-openai-api-key

# ========================================
# Azure OpenAI with Default Credentials (LLM_PROVIDER=azure-openai-identity)
# ========================================
# Uses DefaultAzureCredential which supports:
# - Environment variables (AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID)
# - Managed Identity
# - Azure CLI credentials
# - Visual Studio Code credentials
# No API key needed, just ensure you're logged in via `az login` or have Managed Identity configured

# ========================================
# MCP Server Configuration
# ========================================
# Set to true to enable OBO (On-Behalf-Of) flow for user token authentication
# When true, uses HTTP transport to MCP server and passes user token for Kusto access
# When false, uses stdio transport with DefaultAzureCredential
MCP_USE_OBO_FLOW=false

# URL of the MCP server when using HTTP transport (required when MCP_USE_OBO_FLOW=true)
MCP_SERVER_URL=http://localhost:3001/mcp

# Azure tenant ID for OBO flow
MCP_AZURE_TENANT_ID=your-tenant-id

# Entra App Client ID (the app registration used for authentication)
MCP_ENTRA_APP_CLIENT_ID=your-app-client-id

# User-assigned managed identity client ID (for Azure deployments)
MCP_USER_MANAGED_IDENTITY_CLIENT_ID=your-managed-identity-client-id

# Fabric API base URL
MCP_FABRIC_API_BASE_URL=https://api.fabric.microsoft.com/v1
